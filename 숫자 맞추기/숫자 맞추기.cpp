#include <iostream>
#include <vector>
#include <climits>

using namespace std;

int N;
string init_value, target_value;

int get_distance(char from, char to) {

}

int main() {
	ios::sync_with_stdio(false);	cin.tie(NULL);
	cin >> N >> init_value >> target_value;

	vector<vector<int>> memory(N, vector<int>(N));
	for (int i = 1; i < N; i++) {

	}

	return 0;
}